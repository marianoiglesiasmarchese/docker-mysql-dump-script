# docker-mysql-dump-script
Running dump script at docker compose starting

# Run

```bash
docker-compose up
```

# If you did some change and you wanna see them on the container

```bash
docker rm container_name
docker-compose up --force-recreate --build
```
